extend: default

rules:
  indentation:
    spaces: 2
    indent-sequences: consistent

  line-length:
    max: 120
    level: warning

  trailing-spaces: enable
  new-lines: enable

ignore: |
  charts/**
  .github/**
  templates/**/*.yaml
